'use strict';

// const myDisplay = (text) => {
//   document.getElementById('demo').innerHTML = text;
// };

// const myPromise = new Promise((resolve, reject) => {
//   let x = 210;
//   if (x === 10) {
//     resolve('OK');
//   } else {
//     reject('WRONG');
//   }
// });

// myPromise.then(
//   (value) => myDisplay(value),
//   (error) => myDisplay(error)
// );
